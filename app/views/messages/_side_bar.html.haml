.Side-bar
  .Side-bar__user-name
    .Side-bar__name-space
      .Side-bar__name
        = current_user.name
      .Side-bar__icon
        = link_to new_group_path, class: "Side-bar__edit" do
          = icon('fas', 'edit')
        = link_to edit_user_path(current_user), class: "Side-bar__cog" do 
          = icon('fas', 'cog')

  .Side-bar__group-list
    - current_user.groups.each do |group|
      .Side-bar__group-box
        = link_to '#' do
          .Side-bar__group-name
            = group.name
          .Side-bar__group-message
            メッセージはまだありません
    